package Vista;

import java.awt.EventQueue;
import java.awt.Graphics;
import java.awt.Graphics2D;
import java.awt.Image;
import java.awt.Toolkit;
import java.awt.geom.RoundRectangle2D;
import javax.swing.ImageIcon;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JPanel;
import javax.swing.border.BevelBorder;
import java.awt.Color;
import java.awt.Font;
import javax.swing.SwingConstants;
import javax.swing.JTextField;
import javax.swing.JPasswordField;
import java.awt.event.KeyAdapter;
import java.awt.event.KeyEvent;

public class Login {
    JFrame PL;
    private JLabel fondo;
    private JLabel teresita; 
    private JLabel lblu;
    private JTextField txtu;
    private JLabel creditos;

    public static void main(String[] args) {
        EventQueue.invokeLater(() -> {
            try {
                Login window = new Login();
                window.PL.setVisible(true);
            } catch (Exception e) {
                e.printStackTrace();
            }
        });
    }

    public Login() {
        initialize();
    }

    private void initialize() {
        PL = new JFrame();
        PL.setExtendedState(JFrame.MAXIMIZED_BOTH); // Pantalla completa
        PL.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        PL.getContentPane().setLayout(null);

        // Panel principal con borde redondeado
        JPanel panelForm = new JPanel() {
            @Override
            protected void paintComponent(Graphics g) {
                super.paintComponent(g);
                Graphics2D g2 = (Graphics2D) g;
                g2.setColor(getBackground());
                g2.fill(new RoundRectangle2D.Double(0, 0, getWidth(), getHeight(), 30, 30)); // Esquinas redondeadas
            }
        };
        panelForm.setBackground(new Color(253, 242, 249));
        panelForm.setBorder(new BevelBorder(BevelBorder.LOWERED, new Color(252, 186, 224), new Color(252, 186, 224), new Color(244, 132, 191), new Color(244, 132, 191)));
        
        int panelWidth = 595;
        int panelHeight = 678;
        panelForm.setBounds((Toolkit.getDefaultToolkit().getScreenSize().width - panelWidth) / 2, 
                            (Toolkit.getDefaultToolkit().getScreenSize().height - panelHeight) / 2, 
                            panelWidth, panelHeight);
        PL.getContentPane().add(panelForm);
        panelForm.setLayout(null);

        // Logo
        teresita = new JLabel(""); 
        teresita.setHorizontalAlignment(SwingConstants.CENTER);
        teresita.setBounds(211, 23, 152, 155);
        panelForm.add(teresita);
        Image foto3 = Toolkit.getDefaultToolkit().getImage(getClass().getResource("/img/08TeresitaLogo.png"));
        teresita.setIcon(new ImageIcon(foto3.getScaledInstance(tercero.getWidth(), teresita.getHeight(), Image.SCALE_SMOOTH)));

        // Etiqueta de usuario
        lblu = new JLabel("USUARIO");
        lblu.setForeground(new Color(244, 132, 191));
        lblu.setHorizontalAlignment(SwingConstants.CENTER);
        lblu.setFont(new Font("Dubai", Font.BOLD, 20));
        lblu.setBounds(240, 208, 94, 21);
        panelForm.add(lblu);

        // Campo de texto para el usuario
        txtu = new JTextField();
        txtu.setHorizontalAlignment(SwingConstants.CENTER);
        txtu.addKeyListener(new KeyAdapter() {
            @Override
            public void keyTyped(KeyEvent e) {
                if (txtu.getText().length() >= 50) {
                    e.consume();
                }
            }
        });
        txtu.setFont(new Font("Dialog", Font.PLAIN, 12));
        txtu.setBounds(134, 240, 320, 43);
        panelForm.add(txtu);
        txtu.setColumns(10);

        // Etiqueta de contraseña
        JLabel lblc = new JLabel("CONTRASEÑA");
        lblc.setForeground(new Color(244, 132, 191));
        lblc.setHorizontalAlignment(SwingConstants.CENTER);
        lblc.setFont(new Font("Dubai", Font.BOLD, 20));
        lblc.setBounds(211, 350, 166, 21);
        panelForm.add(lblc);

        // Créditos
        creditos = new JLabel("©2024 PAPELERÍA TERESITA");
        creditos.setForeground(new Color(63, 63, 63));
        creditos.setHorizontalAlignment(SwingConstants.CENTER);
        creditos.setFont(new Font("Dubai", Font.BOLD, 17));
        creditos.setBounds(134, 591, 305, 31);
        panelForm.add(creditos);

        // Fondo
        fondo = new JLabel();
        fondo.setBounds(0, 0, PL.getWidth(), PL.getHeight());
        PL.getContentPane().add(fondo);
        Image foto = Toolkit.getDefaultToolkit().getImage(getClass().getResource("/img/fondo.png"));
        fondo.setIcon(new ImageIcon(foto.getScaledInstance(PL.getWidth(), PL.getHeight(), Image.SCALE_SMOOTH)));

        // Actualizar la imagen de fondo al cambiar el tamaño
        PL.addComponentListener(new java.awt.event.ComponentAdapter() {
            public void componentResized(java.awt.event.ComponentEvent evt) {
                fondo.setBounds(0, 0, PL.getWidth(), PL.getHeight());
                fondo.setIcon(new ImageIcon(foto.getScaledInstance(PL.getWidth(), PL.getHeight(), Image.SCALE_SMOOTH)));
                panelForm.setBounds((PL.getWidth() - panelWidth) / 2, (PL.getHeight() - panelHeight) / 2, panelWidth, panelHeight);
            }
        });
    }
}
